package flints.common.debug;

import flints.common.debug.ParticleFactoryStats;
/**
 * Used to track and display the number of particles being managed by particles emitters.
 */
class ParticleFactoryStats
{
	public static var numParticles:Int = 0;
}
